{
  "api": {
    "host": "{{ sensu.api.host }}",
    "port": {{ sensu.api.port }},
    "user": "{{ sensu.api.username }}",
    "password": "{{ sensu.api.password }}",
	"bind": "{{ sensu.api.bind }}"
  }
}
