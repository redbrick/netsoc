{
  "checks": {
    "base_reboot_check": {
      "handlers": ["default"],
      "command": "check-file-exists.rb -c /var/run/reboot-required",
      "interval": 60,
      "subscribers": [ "servers" ]
    }
  }
}
